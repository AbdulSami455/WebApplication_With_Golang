package main

/*
import (
	"fmt"
	"regexp"
)

func regeexpatterns() {

	// This will reponse true and false depends on pattern
	matched, err := regexp.Match(`foo.*`, []byte(`seafood`))
	fmt.Println(matched, err)

}
*/
